import MeetTeam from '../ui/MeetTeam'
import SectionRule from '../ui/SectionRule'

export default function MeetTeamSection() {
  return (
    <SectionRule id="team" className="bg-[#0A0A0A]">
      <MeetTeam />
    </SectionRule>
  )
}