import InboundProof from '../ui/InboundProof'
import SectionRule from '../ui/SectionRule'

export default function Inbound() {
  return (
    <SectionRule id="inbound" className="bg-[#0A0A0A]">
      <InboundProof />
    </SectionRule>
  )
}