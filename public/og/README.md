# /public/og/

OG images for social link previews (1200×630, PNG).

## Pending

| Page | Expected file | Source template |
|------|---------------|-----------------|
| /adcreator | `adcreator.png` | `/dev/growzillaAssets/launch/adcreator/og-image.html` |

Render with the Puppeteer one-liner in `/dev/growzillaAssets/launch/adcreator/README.md`.

Not committing pre-render placeholder bytes — would just be misleading. A 404 OG image is recoverable; a wrong OG image baked into a Product Hunt post is not.
