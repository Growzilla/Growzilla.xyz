User-agent: *
Allow: /
Disallow: /admin
Disallow: /api/
Disallow: /customapp_launcher
Disallow: /login
Disallow: /signin

# Note: /adcreator/run/* and /adcreator/report/* are transient job pages;
# they carry a <meta name="robots" content="noindex"> tag inline. The main
# /adcreator landing page is intentionally indexable.
#
# /setup and /install-repair are crawler-allowed (so the sitemap entries
# don't conflict) but each page renders <meta name="robots" content="noindex,
# nofollow"> inline, so they will not appear in search results.

Sitemap: https://growzilla.xyz/sitemap.xml
