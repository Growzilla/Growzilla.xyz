/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Homepage: pages/index.tsx — Growzilla content factory landing.
}

module.exports = nextConfig
